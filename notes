/* <progress id="health" value="100" max="100"></progress> */

// // let health = document.querySelector("#health") //Vraag get element by ID
// // health.value -= 30;

// // const btn = document.querySelector("button")

// // let welcomeText = "Welcome to LOVE ME. With LOVE ME you can train your... loving skills! ";
// // let aantalChar = 0;
// // let typingSpeed = 100;
// // let gameText = document.getElementById('typing-effect');

// // function typeText() {
// //     if (aantalChar < welcomeText.length) {
// //         gameText.textContent += welcomeText.charAt(aantalChar);
// //         aantalChar++;
// //     } else {
// //         clearInterval(typingInterval);
// //     }
// // }

// // let typingInterval = setInterval(typeText, typingSpeed);

// // btn.addEventListener("click", typeText)

// document.addEventListener('DOMContentLoaded', function() {
//     let welcomeText = "Welcome to LOVE ME. With LOVE ME you can train your... loving skills! Click on the button (yes the big one down there) to start your loving journey :).";
//     let aantalChar = 0;
//     let typingSpeed = 100;
//     let gameText = document.querySelector('#starttekst');

//     function typeText() {
//         if (aantalChar < welcomeText.length) {
//             gameText.textContent += welcomeText.charAt(aantalChar);
//             aantalChar++;
//         } else {
//             clearInterval(typingInterval);
//         }
//     }

//     let typingInterval = setInterval(typeText, typingSpeed);

//     // Start the typing effect immediately after the page loads
//     typeText();
// });


// if() {

// } else() {

// }







// function ilyBtnClickHandler() {
    // Change the heart image to another one (update the image src)
    heartImg.src = 'images/hearttwo.png'; // Change to the new image path
    
    // Increase the progress bar value by 10%
    let newValue = parseInt(pgressBar.value) + 10;
    if (newValue > 100) newValue = 100; // Ensure it doesn't exceed 100%
    pgressBar.value = newValue;

    // Update the text content of #heartpercent
    pgressPer.textContent = newValue + '%';
}

// Attach the click handler to the ilyBtn
ilyBtn.addEventListener('click', ilyBtnClickHandler);
















// let pgressBar = document.querySelector('#health')
// let pgressPer = document.querySelector('#heartpercent')

// let ilyBtn = document.querySelector('#ily');
// let ydgBtn = document.querySelector('#ydg');
// let ihyBtn = document.querySelector('#ihy');

// let heartImg = document.querySelector('#heart');

// let ylltBtn = document.querySelector('#yllt');
// let yruBtn = document.querySelector('#yru');
// let btsBtn = document.querySelector('#bts');

// let currentImageIndex = 0;

// const heartImages = [
//     'heartone.png',
//     'hearttwo.png',
//     'heartthree.png',
//     'heartfour.png',
//     'heartfive.png',
//     'heartsix.png',
//     'heartseven.png',
//     'hearteight.png',
//     'heartnine.png',
//     'heartten.png',
//     'hearteleven.png'
// ];

// function ilyBtnClickHandler() {
//     // Change the heart image to the next one
//     currentImageIndex++;
//     if (currentImageIndex >= heartImages.length) currentImageIndex = 0;
//     heartImg.src = `images/${heartImages[currentImageIndex]}`;

//     // Increase the progress bar value by 10%
//     let newValue = parseInt(pgressBar.value) + 10;
//     if (newValue > 100) newValue = 100; // Ensure it doesn't exceed 100%

//     // Update the progress bar and percentage text
//     pgressBar.value = newValue;
//     pgressPer.textContent = newValue + '%';

//     // Check if the progress bar is at 100% to reset it
//     if (newValue === 100) {
//         setTimeout(() => {
//             pgressBar.value = 0;
//             pgressPer.textContent = '0%';
//             heartImg.src = 'images/heartone.png'; // Reset the heart image
//             currentImageIndex = 0; // Reset the image index
//         }, 1000); // 1 second delay to display 100% before resetting
//     }
// }

// // Attach the click handler to the ilyBtn
// ilyBtn.addEventListener('click', ilyBtnClickHandler);

// let pgressBar = document.querySelector('#health');
// let pgressPer = document.querySelector('#heartpercent');

// let ilyBtn = document.querySelector('#ily');
// let ydgBtn = document.querySelector('#ydg');
// let ihyBtn = document.querySelector('#ihy');

// let heartImg = document.querySelector('#heart');

// let ylltBtn = document.querySelector('#yllt');
// let yruBtn = document.querySelector('#yru');
// let btsBtn = document.querySelector('#bts');

// let currentImageIndex = 0;

// const heartImages = [
//     'heartone.png',
//     'hearttwo.png',
//     'heartthree.png',
//     'heartfour.png',
//     'heartfive.png',
//     'heartsix.png',
//     'heartseven.png',
//     'hearteight.png',
//     'heartnine.png',
//     'heartten.png',
//     'hearteleven.png'
// ];

// const buttonEffects = {
//     "ily": 10, // i love you - nice
//     "ydg": 10, // you're doing great - nice
//     "ihy": -10, // i hate you - not nice
//     "yllt": 10, // you look lovely today - nice
//     "yru": -10, // you're ugly - not nice
//     "bts": 10 // beautiful smile - nice
// };

// function handleButtonClick(event) {
//     const buttonId = event.target.id;
//     const effect = buttonEffects[buttonId];

//     // Change the heart image to the next one
//     if (effect > 0) {
//         currentImageIndex++;
//         if (currentImageIndex >= heartImages.length) currentImageIndex = 0;
//     } else {
//         currentImageIndex--;
//         if (currentImageIndex < 0) currentImageIndex = heartImages.length - 1;
//     }
//     heartImg.src = `images/${heartImages[currentImageIndex]}`;

//     // Adjust the progress bar value based on the button effect
//     let newValue = parseInt(pgressBar.value) + effect;
//     if (newValue > 100) newValue = 100; // Ensure it doesn't exceed 100%
//     if (newValue < 0) newValue = 0; // Ensure it doesn't go below 0%

//     // Update the progress bar and percentage text
//     pgressBar.value = newValue;
//     pgressPer.textContent = newValue + '%';

//     // Check if the progress bar is at 100% to reset it
//     if (newValue === 100) {
//         setTimeout(() => {
//             pgressBar.value = 0;
//             pgressPer.textContent = '0%';
//             heartImg.src = 'images/heartone.png'; // Reset the heart image
//             currentImageIndex = 0; // Reset the image index
//         }, 1000); // 1 second delay to display 100% before resetting
//     }
// }

// // Attach click handlers to all buttons
// ilyBtn.addEventListener('click', handleButtonClick);
// ydgBtn.addEventListener('click', handleButtonClick);
// ihyBtn.addEventListener('click', handleButtonClick);
// ylltBtn.addEventListener('click', handleButtonClick);
// yruBtn.addEventListener('click', handleButtonClick);
// btsBtn.addEventListener('click', handleButtonClick);